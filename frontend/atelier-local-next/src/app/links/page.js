import UnderConstructionPage from "@/components/UnderConstructionPage";

/**
 * Page des liens utiles.
 *
 * Cette page est actuellement en construction et redirige vers un composant
 * indiquant que la fonctionnalité n'est pas encore disponible.
 *
 * @component
 * @returns {JSX.Element} Composant React affichant la page "en construction".
 */
export default function UsefullLinksPage() {
    // Rendu du composant "UnderConstructionPage" pour signaler que la page est en développement
    return <UnderConstructionPage />;
}